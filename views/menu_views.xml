<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Definir la raíz del menú -->
    <menuitem id="menu_mele_root" name="Mele POS" sequence="1"/>
    <menuitem id="menu_mele_inventory" name="Mele POS" sequence="1"/>

    
    <!-- Definir la acción -->
    <record id="access_mele_invoice" model="ir.actions.act_window">
        <field name="name">Invoices</field>
        <field name="res_model">mele.invoice</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
